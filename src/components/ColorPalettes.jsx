import React from 'react';
import PropTypes from 'prop-types';

const ColorPalettes = () => {
    return <div></div>;
};

ColorPalettes.propTypes = {
};

ColorPalettes.defaultProps = {
};

export default ColorPalettes;